project.version = ''

repositories {
    mavenLocal()
    mavenCentral()
}

sourceSets {
    main.resources.srcDirs = ['src']
}

dependencies {
    implementation (project(':jphp-gui-controlfx-ext')) {
        exclude group: 'org.develnext', module: 'jphp-gui-ext'
    }
}

